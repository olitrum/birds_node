extends layout
block content
  title Sign in
  form()
    div
      label(for='login') Login:
      input(type='text' name='login')
    button(type='submit' onClick = 'viewData()') Submit
  script(src='/main.js')
